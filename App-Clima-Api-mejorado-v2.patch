diff -ruN App-Clima-Api-original/.gitignore App-Clima-Api-mejorado-v2/.gitignore
--- App-Clima-Api-original/.gitignore	1970-01-01 00:00:00.000000000 +0000
+++ App-Clima-Api-mejorado-v2/.gitignore	2026-05-12 04:55:44.408673646 +0000
@@ -0,0 +1,13 @@
+# Sistema
+.DS_Store
+Thumbs.db
+
+# Dependencias y builds futuros
+node_modules/
+dist/
+build/
+
+# Variables locales o llaves privadas
+.env
+.env.*
+config.js
diff -ruN App-Clima-Api-original/README.md App-Clima-Api-mejorado-v2/README.md
--- App-Clima-Api-original/README.md	2026-05-12 04:55:44.314871001 +0000
+++ App-Clima-Api-mejorado-v2/README.md	2026-05-12 04:55:44.422706727 +0000
@@ -1,23 +1,125 @@
+<div align="center">
+
 # 🌦️ App del Clima
 
-Aplicación web que permite consultar el clima en cualquier ciudad en tiempo real.
+Una app web moderna para consultar el clima por ciudad o ubicación actual, con fondos dinámicos, animaciones, pronóstico de 5 días y JavaScript modular.
+
+[![HTML](https://img.shields.io/badge/HTML-5-E34F26?style=for-the-badge&logo=html5&logoColor=white)](#)
+[![CSS](https://img.shields.io/badge/CSS-3-1572B6?style=for-the-badge&logo=css3&logoColor=white)](#)
+[![JavaScript](https://img.shields.io/badge/JavaScript-ES6_Modules-F7DF1E?style=for-the-badge&logo=javascript&logoColor=111)](#)
+[![API](https://img.shields.io/badge/API-Open--Meteo-38BDF8?style=for-the-badge)](#)
+
+[🌐 Ver demo](https://app-clima-api2.netlify.app) · [🐛 Reportar problema](../../issues) · [✨ Sugerir mejora](../../issues)
+
+</div>
+
+![Vista previa de la App del Clima](assets/preview.svg)
+
+## ✨ Características
+
+- 🔎 Búsqueda por ciudad con botón, tecla Enter y accesos rápidos.
+- 📍 Consulta por ubicación actual usando geolocalización del navegador.
+- 🌡️ Temperatura, sensación térmica, humedad, viento y dirección del viento.
+- 📅 Pronóstico visual de 5 días.
+- 🎨 Fondos dinámicos según el clima: soleado, lluvia, tormenta, nieve, niebla y noche.
+- 🪄 Animaciones suaves: tarjetas flotantes, nubes, lluvia, brillo y loader animado.
+- 📱 Diseño responsive para celular, tablet y escritorio.
+- ♿ Mejoras de accesibilidad con etiquetas, `aria-live` y soporte para reducir movimiento.
+- 🔐 Sin API key hardcodeada: usa Open-Meteo, una API pública que no requiere llave.
+- 🧱 Código más limpio con módulos ES6 separados por responsabilidad.
+
+## 🧰 Tecnologías
+
+| Tecnología | Uso |
+|---|---|
+| HTML5 | Estructura semántica |
+| CSS3 | Glassmorphism, animaciones y responsive design |
+| JavaScript ES Modules | Lógica separada y mantenible |
+| Open-Meteo | Geocoding y clima actual con pronóstico |
+| Netlify | Despliegue estático |
+
+## 📁 Estructura
+
+```txt
+App-Clima-Api/
+├── assets/
+│   ├── favicon.svg
+│   └── preview.svg
+├── js/
+│   ├── app.js
+│   ├── config.js
+│   ├── ui.js
+│   ├── utils.js
+│   ├── weather-api.js
+│   └── weather-codes.js
+├── index.html
+├── style.css
+├── netlify.toml
+├── .gitignore
+└── README.md
+```
+
+## 🧱 Organización del código
+
+| Archivo | Responsabilidad |
+|---|---|
+| `js/app.js` | Inicializa la app, escucha eventos y coordina búsquedas |
+| `js/weather-api.js` | Consume las APIs de ciudad y clima |
+| `js/ui.js` | Renderiza estados de carga, error y resultados |
+| `js/weather-codes.js` | Traduce códigos meteorológicos a iconos, textos y temas visuales |
+| `js/utils.js` | Formatos, escape HTML, URLs y utilidades reutilizables |
+| `js/config.js` | Configuración general, endpoints y parámetros de la API |
+
+## 🚀 Cómo ejecutar localmente
+
+1. Clona el repositorio:
+
+```bash
+git clone https://github.com/angelcamayojm-wq/App-Clima-Api.git
+cd App-Clima-Api
+```
+
+2. Abre `index.html` con Live Server en VS Code.
+
+> Nota: como se usan módulos ES6, es mejor abrirlo con Live Server y no directo con doble clic, porque algunos navegadores bloquean módulos desde `file://`.
+
+## 🌐 APIs usadas
+
+La app usa dos endpoints públicos de Open-Meteo:
+
+- **Geocoding API**: convierte el nombre de la ciudad en coordenadas.
+- **Forecast API**: obtiene clima actual y pronóstico de 5 días.
+
+No necesitas crear cuenta ni guardar una API key en el proyecto.
+
+## 🧠 Aprendizajes del proyecto
+
+Este proyecto ayuda a practicar:
+
+- Consumo de APIs con `fetch` y `async/await`.
+- Separación de responsabilidades en JavaScript.
+- Manejo de errores para búsquedas fallidas, conexión o permisos de ubicación.
+- Manipulación del DOM con plantillas dinámicas.
+- Responsive design con CSS Grid y media queries.
+- Accesibilidad básica en formularios y estados de carga.
+
+## 🛣️ Próximas mejoras sugeridas
+
+- Guardar la última ciudad buscada con `localStorage`.
+- Agregar selector °C / °F.
+- Mostrar hora de salida y puesta del sol.
+- Crear historial de búsquedas recientes.
+- Añadir tests básicos para funciones de formato.
+
+## 👨‍💻 Autor
+
+Creado por [@angelcamayojm-wq](https://github.com/angelcamayojm-wq).  
+Mejorado visualmente y refactorizado con módulos para que el código sea más claro.
 
-## 🚀 Tecnologías
-- HTML
-- CSS
-- JavaScript
-- API OpenWeather
-
-## ✨ Funcionalidades
-- Buscar ciudad
-- Mostrar temperatura, clima y humedad
-- Íconos dinámicos según el clima
-- Cambio de fondo según condiciones
-- Búsqueda con botón y tecla Enter
+---
 
-## 🌐 Demo
-https://app-clima-api2.netlify.app
+<div align="center">
 
+Hecho con café, clima raro y JavaScript ordenadito. ☕🌦️
 
-## 📚 Aprendizaje
-Este proyecto me permitió practicar consumo de APIs, manejo de eventos y manipulación del DOM.
+</div>
diff -ruN App-Clima-Api-original/assets/favicon.svg App-Clima-Api-mejorado-v2/assets/favicon.svg
--- App-Clima-Api-original/assets/favicon.svg	1970-01-01 00:00:00.000000000 +0000
+++ App-Clima-Api-mejorado-v2/assets/favicon.svg	2026-05-12 04:55:44.393891982 +0000
@@ -0,0 +1,6 @@
+<svg width="96" height="96" viewBox="0 0 96 96" fill="none" xmlns="http://www.w3.org/2000/svg">
+  <rect width="96" height="96" rx="24" fill="url(#bg)"/>
+  <circle cx="35" cy="34" r="16" fill="#FACC15"/>
+  <path d="M23 62c0-9.4 7.6-17 17-17 5.9 0 11.2 3 14.2 7.8A16.4 16.4 0 0 1 61 51c9.4 0 17 7.6 17 17H29a6 6 0 0 1-6-6Z" fill="white" fill-opacity=".94"/>
+  <defs><linearGradient id="bg" x1="9" y1="10" x2="84" y2="89" gradientUnits="userSpaceOnUse"><stop stop-color="#38BDF8"/><stop offset="1" stop-color="#1D4ED8"/></linearGradient></defs>
+</svg>
diff -ruN App-Clima-Api-original/assets/preview.svg App-Clima-Api-mejorado-v2/assets/preview.svg
--- App-Clima-Api-original/assets/preview.svg	1970-01-01 00:00:00.000000000 +0000
+++ App-Clima-Api-mejorado-v2/assets/preview.svg	2026-05-12 04:55:44.401585036 +0000
@@ -0,0 +1,36 @@
+<svg width="1280" height="720" viewBox="0 0 1280 720" fill="none" xmlns="http://www.w3.org/2000/svg">
+  <rect width="1280" height="720" fill="url(#bg)"/>
+  <circle cx="1040" cy="130" r="118" fill="url(#sun)"/>
+  <circle cx="140" cy="620" r="210" fill="#38BDF8" fill-opacity=".22"/>
+  <path d="M0 0h1280v720H0z" fill="url(#grid)" fill-opacity=".17"/>
+  <g filter="url(#shadow)">
+    <rect x="116" y="116" width="492" height="488" rx="36" fill="#0F172A" fill-opacity=".62" stroke="white" stroke-opacity=".18"/>
+    <text x="162" y="186" fill="#F8FAFC" font-family="Arial, sans-serif" font-size="24" font-weight="700">Clima en tiempo real</text>
+    <text x="162" y="275" fill="#F8FAFC" font-family="Arial, sans-serif" font-size="76" font-weight="900">App del</text>
+    <text x="162" y="352" fill="#F8FAFC" font-family="Arial, sans-serif" font-size="76" font-weight="900">Clima</text>
+    <rect x="162" y="404" width="344" height="64" rx="18" fill="#0F172A" fill-opacity=".58" stroke="white" stroke-opacity=".18"/>
+    <text x="188" y="444" fill="#CBD5E1" font-family="Arial, sans-serif" font-size="24">Buscar ciudad...</text>
+    <rect x="162" y="496" width="92" height="38" rx="19" fill="white" fill-opacity=".12"/>
+    <text x="183" y="522" fill="#F8FAFC" font-family="Arial, sans-serif" font-size="18" font-weight="700">Cali</text>
+    <rect x="268" y="496" width="132" height="38" rx="19" fill="white" fill-opacity=".12"/>
+    <text x="290" y="522" fill="#F8FAFC" font-family="Arial, sans-serif" font-size="18" font-weight="700">Medellín</text>
+  </g>
+  <g filter="url(#shadow)">
+    <rect x="672" y="116" width="492" height="488" rx="36" fill="#0F172A" fill-opacity=".64" stroke="white" stroke-opacity=".18"/>
+    <rect x="718" y="156" width="128" height="128" rx="32" fill="white" fill-opacity=".14"/>
+    <text x="742" y="240" font-family="Arial, sans-serif" font-size="68">🌤️</text>
+    <text x="872" y="200" fill="#F8FAFC" font-family="Arial, sans-serif" font-size="50" font-weight="900">Bogotá</text>
+    <text x="872" y="240" fill="#CBD5E1" font-family="Arial, sans-serif" font-size="24">Parcialmente nublado</text>
+    <text x="718" y="394" fill="#F8FAFC" font-family="Arial, sans-serif" font-size="112" font-weight="900">19°C</text>
+    <g fill="white" fill-opacity=".12" stroke="white" stroke-opacity=".16">
+      <rect x="718" y="438" width="92" height="86" rx="18"/><rect x="826" y="438" width="92" height="86" rx="18"/><rect x="934" y="438" width="92" height="86" rx="18"/><rect x="1042" y="438" width="76" height="86" rx="18"/>
+    </g>
+    <text x="744" y="489" font-family="Arial, sans-serif" font-size="32">💧</text><text x="852" y="489" font-family="Arial, sans-serif" font-size="32">💨</text><text x="960" y="489" font-family="Arial, sans-serif" font-size="32">🥵</text><text x="1060" y="489" font-family="Arial, sans-serif" font-size="32">🧭</text>
+  </g>
+  <defs>
+    <linearGradient id="bg" x1="0" y1="0" x2="1280" y2="720" gradientUnits="userSpaceOnUse"><stop stop-color="#0F172A"/><stop offset=".55" stop-color="#0369A1"/><stop offset="1" stop-color="#38BDF8"/></linearGradient>
+    <radialGradient id="sun" cx="0" cy="0" r="1" gradientUnits="userSpaceOnUse" gradientTransform="translate(1040 130) rotate(90) scale(118)"><stop stop-color="#FEF08A"/><stop offset=".55" stop-color="#FB923C" stop-opacity=".5"/><stop offset="1" stop-color="#FB923C" stop-opacity="0"/></radialGradient>
+    <pattern id="grid" width="42" height="42" patternUnits="userSpaceOnUse"><path d="M42 0H0v42" stroke="white" stroke-opacity=".35"/></pattern>
+    <filter id="shadow" x="56" y="66" width="1168" height="598" filterUnits="userSpaceOnUse" color-interpolation-filters="sRGB"><feDropShadow dx="0" dy="30" stdDeviation="28" flood-color="#020617" flood-opacity=".35"/></filter>
+  </defs>
+</svg>
diff -ruN App-Clima-Api-original/index.html App-Clima-Api-mejorado-v2/index.html
--- App-Clima-Api-original/index.html	2026-05-12 04:55:44.321913370 +0000
+++ App-Clima-Api-mejorado-v2/index.html	2026-05-12 04:55:44.343111725 +0000
@@ -3,21 +3,74 @@
 <head>
   <meta charset="UTF-8">
   <meta name="viewport" content="width=device-width, initial-scale=1.0">
-  <title>App del Clima</title>
+  <meta name="description" content="App del clima moderna con animaciones, código modular y pronóstico de 5 días.">
+  <meta name="theme-color" content="#0ea5e9">
+  <title>App del Clima | Consulta el clima en vivo</title>
+  <link rel="icon" type="image/svg+xml" href="assets/favicon.svg">
+  <link rel="preconnect" href="https://api.open-meteo.com">
+  <link rel="preconnect" href="https://geocoding-api.open-meteo.com">
   <link rel="stylesheet" href="style.css">
 </head>
-<body>
-  <div class="contenedor">
-    <h1>App del Clima</h1>
-
-    <div class="busqueda">
-      <input type="text" id="inputCiudad" placeholder="Escribe una ciudad">
-      <button id="btnBuscar">Buscar</button>
-    </div>
-
-    <div id="resultado"></div>
+<body data-weather="default">
+  <div class="weather-scene" aria-hidden="true">
+    <span class="orb orb-sun"></span>
+    <span class="orb orb-glow"></span>
+    <span class="cloud cloud-one"></span>
+    <span class="cloud cloud-two"></span>
+    <span class="cloud cloud-three"></span>
+    <span class="rain rain-one"></span>
+    <span class="rain rain-two"></span>
+    <span class="sparkle sparkle-one"></span>
+    <span class="sparkle sparkle-two"></span>
+    <span class="sparkle sparkle-three"></span>
   </div>
 
-  <script src="script.js"></script>
+  <main class="app-shell">
+    <section class="hero-card" aria-labelledby="tituloApp">
+      <p class="eyebrow">Clima en tiempo real</p>
+      <h1 id="tituloApp">App del Clima</h1>
+      <p class="hero-copy">
+        Busca cualquier ciudad y mira temperatura, sensación térmica, humedad, viento y pronóstico de 5 días con una interfaz más viva.
+      </p>
+
+      <form class="search-card" id="formBusqueda" autocomplete="off">
+        <label class="sr-only" for="inputCiudad">Ciudad</label>
+        <div class="input-wrap">
+          <span aria-hidden="true">🔎</span>
+          <input type="search" id="inputCiudad" name="ciudad" placeholder="Ej: Bogotá, Cali, Madrid" spellcheck="false" required>
+        </div>
+        <button type="submit" id="btnBuscar">
+          <span>Buscar</span>
+          <span aria-hidden="true">⚡</span>
+        </button>
+      </form>
+
+      <div class="quick-actions" aria-label="Búsquedas rápidas">
+        <button type="button" class="chip" data-city="Bogotá">Bogotá</button>
+        <button type="button" class="chip" data-city="Medellín">Medellín</button>
+        <button type="button" class="chip" data-city="Cali">Cali</button>
+        <button type="button" class="chip" data-city="London">London</button>
+      </div>
+
+      <button type="button" class="location-button" id="btnUbicacion">
+        <span aria-hidden="true">📍</span>
+        Usar mi ubicación
+      </button>
+    </section>
+
+    <section id="resultado" class="result-card is-empty" aria-live="polite" aria-busy="false">
+      <div class="empty-state">
+        <span class="empty-icon" aria-hidden="true">🌤️</span>
+        <h2>¿Dónde está el clima?</h2>
+        <p>Escribe una ciudad para invocar el pronóstico. Sí, meteorología con estilo, qué elegancia.</p>
+      </div>
+    </section>
+  </main>
+
+  <footer class="site-footer">
+    Hecho con HTML, CSS, JavaScript modular y Open-Meteo API.
+  </footer>
+
+  <script type="module" src="js/app.js"></script>
 </body>
 </html>
diff -ruN App-Clima-Api-original/js/app.js App-Clima-Api-mejorado-v2/js/app.js
--- App-Clima-Api-original/js/app.js	1970-01-01 00:00:00.000000000 +0000
+++ App-Clima-Api-mejorado-v2/js/app.js	2026-05-12 04:55:44.387562139 +0000
@@ -0,0 +1,127 @@
+import { APP_CONFIG } from "./config.js";
+import { getForecast, searchCity } from "./weather-api.js";
+import { $, $$, getReadableError } from "./utils.js";
+import { renderError, renderLoading, renderWeather } from "./ui.js";
+
+const state = {
+  requestController: null
+};
+
+const elements = {
+  form: $("#formBusqueda"),
+  input: $("#inputCiudad"),
+  result: $("#resultado"),
+  searchButton: $("#btnBuscar"),
+  locationButton: $("#btnUbicacion"),
+  chips: $$(".chip")
+};
+
+window.addEventListener("DOMContentLoaded", initApp);
+
+function initApp() {
+  elements.form.addEventListener("submit", handleSubmit);
+  elements.locationButton.addEventListener("click", handleLocationSearch);
+  elements.chips.forEach((chip) => chip.addEventListener("click", () => handleChipClick(chip)));
+
+  elements.input.value = APP_CONFIG.defaultCity;
+  handleCitySearch(APP_CONFIG.defaultCity);
+}
+
+function handleSubmit(event) {
+  event.preventDefault();
+  handleCitySearch(elements.input.value.trim());
+}
+
+function handleChipClick(chip) {
+  const city = chip.dataset.city;
+  elements.input.value = city;
+  handleCitySearch(city);
+}
+
+async function handleCitySearch(city) {
+  if (!city) {
+    renderError(
+      elements.result,
+      "Escribe una ciudad",
+      "Necesito una ciudad para consultar el clima. Mi bola de cristal está en mantenimiento."
+    );
+    return;
+  }
+
+  const controller = resetRequestController();
+  renderLoading(elements.result, `Buscando clima en ${city}`);
+  setControlsDisabled(true);
+
+  try {
+    const place = await searchCity(city, { signal: controller.signal });
+    const forecast = await getForecast(place, { signal: controller.signal });
+    renderWeather(elements.result, forecast, place);
+  } catch (error) {
+    const message = getReadableError(error, "No pude obtener el clima.");
+    if (message) renderError(elements.result, "No pude obtener el clima", message);
+  } finally {
+    if (state.requestController === controller) setControlsDisabled(false);
+  }
+}
+
+async function handleLocationSearch() {
+  if (!navigator.geolocation) {
+    renderError(elements.result, "Ubicación no disponible", "Tu navegador no soporta geolocalización.");
+    return;
+  }
+
+  const controller = resetRequestController();
+  renderLoading(elements.result, "Detectando tu ubicación");
+  setControlsDisabled(true);
+
+  try {
+    const { coords } = await getCurrentPosition(APP_CONFIG.geolocationOptions);
+    const place = createGpsPlace(coords);
+    const forecast = await getForecast(place, { signal: controller.signal });
+    renderWeather(elements.result, forecast, place);
+  } catch (error) {
+    const message = error?.code ? getGeolocationErrorMessage(error) : getReadableError(error, "No pude leer tu clima.");
+    if (message) renderError(elements.result, "No pude leer tu ubicación", message);
+  } finally {
+    if (state.requestController === controller) setControlsDisabled(false);
+  }
+}
+
+function resetRequestController() {
+  state.requestController?.abort();
+  state.requestController = new AbortController();
+  return state.requestController;
+}
+
+function setControlsDisabled(isDisabled) {
+  elements.searchButton.disabled = isDisabled;
+  elements.locationButton.disabled = isDisabled;
+  elements.chips.forEach((chip) => {
+    chip.disabled = isDisabled;
+  });
+}
+
+function getCurrentPosition(options) {
+  return new Promise((resolve, reject) => {
+    navigator.geolocation.getCurrentPosition(resolve, reject, options);
+  });
+}
+
+function createGpsPlace({ latitude, longitude }) {
+  return {
+    name: "Tu ubicación",
+    country: "GPS",
+    latitude,
+    longitude
+  };
+}
+
+function getGeolocationErrorMessage(error) {
+  const messages = {
+    1: "Permiso denegado. Activa la ubicación o escribe una ciudad manualmente.",
+    2: "No pude detectar tu posición. Revisa el GPS o intenta escribir una ciudad.",
+    3: "La ubicación tardó demasiado. El GPS se fue por empanadas."
+  };
+
+  return messages[error.code] ?? "No pude detectar tu ubicación.";
+}
diff -ruN App-Clima-Api-original/js/config.js App-Clima-Api-mejorado-v2/js/config.js
--- App-Clima-Api-original/js/config.js	1970-01-01 00:00:00.000000000 +0000
+++ App-Clima-Api-mejorado-v2/js/config.js	2026-05-12 04:55:44.355502669 +0000
@@ -0,0 +1,33 @@
+export const APP_CONFIG = Object.freeze({
+  defaultCity: "Bogotá",
+  locale: "es-CO",
+  forecastDays: 5,
+  geolocationOptions: Object.freeze({
+    enableHighAccuracy: true,
+    timeout: 10000,
+    maximumAge: 300000
+  }),
+  endpoints: Object.freeze({
+    geocoding: "https://geocoding-api.open-meteo.com/v1/search",
+    forecast: "https://api.open-meteo.com/v1/forecast"
+  })
+});
+
+export const WEATHER_PARAMS = Object.freeze({
+  current: [
+    "temperature_2m",
+    "relative_humidity_2m",
+    "apparent_temperature",
+    "weather_code",
+    "wind_speed_10m",
+    "wind_direction_10m",
+    "is_day",
+    "precipitation"
+  ],
+  daily: [
+    "weather_code",
+    "temperature_2m_max",
+    "temperature_2m_min",
+    "precipitation_probability_max"
+  ]
+});
diff -ruN App-Clima-Api-original/js/ui.js App-Clima-Api-mejorado-v2/js/ui.js
--- App-Clima-Api-original/js/ui.js	1970-01-01 00:00:00.000000000 +0000
+++ App-Clima-Api-mejorado-v2/js/ui.js	2026-05-12 04:55:44.381397210 +0000
@@ -0,0 +1,112 @@
+import { getWeatherInfo } from "./weather-codes.js";
+import { escapeHTML, formatDate, formatTime, round, windDirection } from "./utils.js";
+
+export function setTheme(theme = "default") {
+  document.body.dataset.weather = theme;
+}
+
+export function renderLoading(container, message) {
+  container.classList.remove("is-empty");
+  container.setAttribute("aria-busy", "true");
+  container.innerHTML = `
+    <div class="loading-state" role="status">
+      <div>
+        <div class="loader" aria-hidden="true"></div>
+        <h2>${escapeHTML(message)}</h2>
+        <div class="skeleton-line"></div>
+        <div class="skeleton-line"></div>
+      </div>
+    </div>
+  `;
+}
+
+export function renderError(container, title, message) {
+  setTheme("default");
+  container.classList.remove("is-empty");
+  container.setAttribute("aria-busy", "false");
+  container.innerHTML = `
+    <div class="error-state" role="alert">
+      <div>
+        <span class="error-icon" aria-hidden="true">🌩️</span>
+        <h2>${escapeHTML(title)}</h2>
+        <p>${escapeHTML(message)}</p>
+      </div>
+    </div>
+  `;
+}
+
+export function renderWeather(container, forecast, place) {
+  const current = forecast.current;
+  const weather = getWeatherInfo(current.weather_code, current.is_day);
+
+  setTheme(weather.theme);
+  container.classList.remove("is-empty");
+  container.setAttribute("aria-busy", "false");
+  container.innerHTML = weatherTemplate({ current, forecast, place, weather });
+}
+
+function weatherTemplate({ current, forecast, place, weather }) {
+  const location = getLocationName(place);
+
+  return `
+    <article class="weather-current">
+      <div class="weather-main">
+        <div class="weather-icon" aria-hidden="true">${weather.icon}</div>
+        <div>
+          <h2 class="city-name">${escapeHTML(location)}</h2>
+          <p class="weather-description">${weather.text}</p>
+        </div>
+      </div>
+
+      <div class="temperature-row">
+        <p class="temperature">${round(current.temperature_2m)}<sup>°C</sup></p>
+        <p class="updated-at">Actualizado ${formatTime(current.time)}</p>
+      </div>
+
+      <div class="details-grid">
+        ${detailCard("🥵", "Sensación", `${round(current.apparent_temperature)} °C`)}
+        ${detailCard("💧", "Humedad", `${current.relative_humidity_2m}%`)}
+        ${detailCard("💨", "Viento", `${round(current.wind_speed_10m)} km/h`)}
+        ${detailCard("🧭", "Dirección", windDirection(current.wind_direction_10m))}
+      </div>
+
+      <h3 class="forecast-title">Pronóstico de 5 días</h3>
+      <div class="forecast-grid">
+        ${forecastTemplate(forecast.daily)}
+      </div>
+    </article>
+  `;
+}
+
+function detailCard(icon, label, value) {
+  return `
+    <div class="detail-card">
+      <p class="detail-label"><span aria-hidden="true">${icon}</span>${label}</p>
+      <p class="detail-value">${value}</p>
+    </div>
+  `;
+}
+
+function forecastTemplate(daily) {
+  return daily.time.map((date, index) => forecastDayTemplate(daily, date, index)).join("");
+}
+
+function forecastDayTemplate(daily, date, index) {
+  const weather = getWeatherInfo(daily.weather_code[index], true);
+  const max = round(daily.temperature_2m_max[index]);
+  const min = round(daily.temperature_2m_min[index]);
+  const rainChance = daily.precipitation_probability_max[index] ?? 0;
+
+  return `
+    <article class="forecast-day" title="${weather.text}">
+      <time datetime="${date}">${formatDate(date)}</time>
+      <span class="forecast-icon" aria-hidden="true">${weather.icon}</span>
+      <strong>${max}° / ${min}°</strong>
+      <small>💧 ${rainChance}%</small>
+    </article>
+  `;
+}
+
+function getLocationName(place) {
+  return [place.name, place.admin1, place.country].filter(Boolean).join(", ");
+}
diff -ruN App-Clima-Api-original/js/utils.js App-Clima-Api-mejorado-v2/js/utils.js
--- App-Clima-Api-original/js/utils.js	1970-01-01 00:00:00.000000000 +0000
+++ App-Clima-Api-mejorado-v2/js/utils.js	2026-05-12 04:55:44.361686099 +0000
@@ -0,0 +1,65 @@
+import { APP_CONFIG } from "./config.js";
+
+const HTML_ESCAPES = Object.freeze({
+  "&": "&amp;",
+  "<": "&lt;",
+  ">": "&gt;",
+  "'": "&#39;",
+  '"': "&quot;"
+});
+
+export function $(selector, parent = document) {
+  return parent.querySelector(selector);
+}
+
+export function $$(selector, parent = document) {
+  return [...parent.querySelectorAll(selector)];
+}
+
+export function buildUrl(baseUrl, params = {}) {
+  const url = new URL(baseUrl);
+
+  Object.entries(params).forEach(([key, value]) => {
+    if (value === null || value === undefined || value === "") return;
+    const formattedValue = Array.isArray(value) ? value.join(",") : String(value);
+    url.searchParams.set(key, formattedValue);
+  });
+
+  return url;
+}
+
+export function escapeHTML(value) {
+  return String(value).replace(/[&<>'"]/g, (character) => HTML_ESCAPES[character]);
+}
+
+export function formatDate(value, options = {}) {
+  return new Intl.DateTimeFormat(APP_CONFIG.locale, {
+    weekday: "short",
+    day: "numeric",
+    month: "short",
+    ...options
+  }).format(new Date(`${value}T00:00:00`));
+}
+
+export function formatTime(value, options = {}) {
+  return new Intl.DateTimeFormat(APP_CONFIG.locale, {
+    hour: "2-digit",
+    minute: "2-digit",
+    ...options
+  }).format(new Date(value));
+}
+
+export function round(value) {
+  return Math.round(Number(value));
+}
+
+export function windDirection(degrees = 0) {
+  const directions = ["N", "NE", "E", "SE", "S", "SO", "O", "NO"];
+  const index = Math.round(Number(degrees) / 45) % directions.length;
+  return directions[index];
+}
+
+export function getReadableError(error, fallbackMessage) {
+  if (error?.name === "AbortError") return "";
+  return error?.message || fallbackMessage;
+}
diff -ruN App-Clima-Api-original/js/weather-api.js App-Clima-Api-mejorado-v2/js/weather-api.js
--- App-Clima-Api-original/js/weather-api.js	1970-01-01 00:00:00.000000000 +0000
+++ App-Clima-Api-mejorado-v2/js/weather-api.js	2026-05-12 04:55:44.374925160 +0000
@@ -0,0 +1,48 @@
+import { APP_CONFIG, WEATHER_PARAMS } from "./config.js";
+import { buildUrl } from "./utils.js";
+
+export async function searchCity(city, { signal } = {}) {
+  const url = buildUrl(APP_CONFIG.endpoints.geocoding, {
+    name: city,
+    count: 1,
+    language: "es",
+    format: "json"
+  });
+
+  const data = await fetchJson(url, {
+    signal,
+    errorMessage: "El servicio de búsqueda de ciudades no respondió bien."
+  });
+
+  if (!data.results?.length) {
+    throw new Error(`No encontré una ciudad llamada "${city}". Revisa la escritura e intenta de nuevo.`);
+  }
+
+  return data.results[0];
+}
+
+export async function getForecast({ latitude, longitude }, { signal } = {}) {
+  const url = buildUrl(APP_CONFIG.endpoints.forecast, {
+    latitude,
+    longitude,
+    current: WEATHER_PARAMS.current,
+    daily: WEATHER_PARAMS.daily,
+    timezone: "auto",
+    forecast_days: APP_CONFIG.forecastDays
+  });
+
+  return fetchJson(url, {
+    signal,
+    errorMessage: "El servicio del clima no respondió. Puede ser conexión o la API tomando cafecito."
+  });
+}
+
+async function fetchJson(url, { signal, errorMessage }) {
+  const response = await fetch(url, { signal });
+
+  if (!response.ok) {
+    throw new Error(errorMessage);
+  }
+
+  return response.json();
+}
diff -ruN App-Clima-Api-original/js/weather-codes.js App-Clima-Api-mejorado-v2/js/weather-codes.js
--- App-Clima-Api-original/js/weather-codes.js	1970-01-01 00:00:00.000000000 +0000
+++ App-Clima-Api-mejorado-v2/js/weather-codes.js	2026-05-12 04:55:44.368005619 +0000
@@ -0,0 +1,48 @@
+const WEATHER_CODES = new Map([
+  [0, { icon: "☀️", text: "cielo despejado", theme: "sunny" }],
+  [1, { icon: "🌤️", text: "principalmente despejado", theme: "sunny" }],
+  [2, { icon: "⛅", text: "parcialmente nublado", theme: "cloudy" }],
+  [3, { icon: "☁️", text: "nublado", theme: "cloudy" }],
+  [45, { icon: "🌫️", text: "niebla", theme: "foggy" }],
+  [48, { icon: "🌫️", text: "niebla con escarcha", theme: "foggy" }],
+  [51, { icon: "🌦️", text: "llovizna ligera", theme: "rainy" }],
+  [53, { icon: "🌦️", text: "llovizna moderada", theme: "rainy" }],
+  [55, { icon: "🌧️", text: "llovizna intensa", theme: "rainy" }],
+  [56, { icon: "🌧️", text: "llovizna helada ligera", theme: "rainy" }],
+  [57, { icon: "🌧️", text: "llovizna helada intensa", theme: "rainy" }],
+  [61, { icon: "🌧️", text: "lluvia ligera", theme: "rainy" }],
+  [63, { icon: "🌧️", text: "lluvia moderada", theme: "rainy" }],
+  [65, { icon: "🌧️", text: "lluvia intensa", theme: "rainy" }],
+  [66, { icon: "🌧️", text: "lluvia helada ligera", theme: "rainy" }],
+  [67, { icon: "🌧️", text: "lluvia helada intensa", theme: "rainy" }],
+  [71, { icon: "🌨️", text: "nieve ligera", theme: "snowy" }],
+  [73, { icon: "🌨️", text: "nieve moderada", theme: "snowy" }],
+  [75, { icon: "❄️", text: "nieve intensa", theme: "snowy" }],
+  [77, { icon: "❄️", text: "granos de nieve", theme: "snowy" }],
+  [80, { icon: "🌦️", text: "chubascos ligeros", theme: "rainy" }],
+  [81, { icon: "🌧️", text: "chubascos moderados", theme: "rainy" }],
+  [82, { icon: "⛈️", text: "chubascos fuertes", theme: "stormy" }],
+  [85, { icon: "🌨️", text: "chubascos de nieve ligeros", theme: "snowy" }],
+  [86, { icon: "❄️", text: "chubascos de nieve fuertes", theme: "snowy" }],
+  [95, { icon: "⛈️", text: "tormenta", theme: "stormy" }],
+  [96, { icon: "⛈️", text: "tormenta con granizo ligero", theme: "stormy" }],
+  [99, { icon: "⛈️", text: "tormenta con granizo fuerte", theme: "stormy" }]
+]);
+
+const DEFAULT_WEATHER = Object.freeze({ icon: "🌍", text: "clima variable", theme: "default" });
+const NIGHT_CODES = new Set([0, 1, 2]);
+
+export function getWeatherInfo(code, isDay = true) {
+  const normalizedCode = Number(code);
+  const weather = WEATHER_CODES.get(normalizedCode) ?? DEFAULT_WEATHER;
+
+  if (!Boolean(isDay) && NIGHT_CODES.has(normalizedCode)) {
+    return {
+      ...weather,
+      icon: normalizedCode === 0 ? "🌙" : "☁️",
+      theme: "night"
+    };
+  }
+
+  return weather;
+}
diff -ruN App-Clima-Api-original/netlify.toml App-Clima-Api-mejorado-v2/netlify.toml
--- App-Clima-Api-original/netlify.toml	1970-01-01 00:00:00.000000000 +0000
+++ App-Clima-Api-mejorado-v2/netlify.toml	2026-05-12 04:55:44.415182164 +0000
@@ -0,0 +1,7 @@
+[[headers]]
+  for = "/*"
+  [headers.values]
+    X-Frame-Options = "DENY"
+    X-Content-Type-Options = "nosniff"
+    Referrer-Policy = "strict-origin-when-cross-origin"
+    Permissions-Policy = "geolocation=(self)"
diff -ruN App-Clima-Api-original/script.js App-Clima-Api-mejorado-v2/script.js
--- App-Clima-Api-original/script.js	2026-05-12 04:55:44.336007654 +0000
+++ App-Clima-Api-mejorado-v2/script.js	1970-01-01 00:00:00.000000000 +0000
@@ -1,87 +0,0 @@
-const inputCiudad = document.getElementById("inputCiudad");
-const btnBuscar = document.getElementById("btnBuscar");
-const resultado = document.getElementById("resultado");
-
-const apiKey = "15e81241dc1a2c9cd032fb7471896c20";
-
-btnBuscar.addEventListener("click", async function () {
-  const ciudad = inputCiudad.value.trim();
-
-  if (ciudad === "") {
-    alert("Escribe una ciudad");
-    return;
-  }
-
-  resultado.innerHTML = "";
-  resultado.innerHTML = "Cargando... ⏳";
-
-  try {
-    const url = `https://api.openweathermap.org/data/2.5/weather?q=${ciudad}&appid=${apiKey}&units=metric&lang=es`;
-
-    const respuesta = await fetch(url);
-    const data = await respuesta.json();
-
-    if (data.cod !== 200) {
-      resultado.innerHTML = `Error: ${data.message}`;
-      return;
-    }
-
-    let icono = "🌍";
-let fondo = "#f4f4f4";
-
-let climaTexto = data.weather[0].description.toLowerCase();
-
-if (data.weather[0].main === "Clear") {
-  icono = "☀️";
-  fondo = "#FFD54F";
-} else if (data.weather[0].main === "Clouds") {
-  icono = "☁️";
-  fondo = "#90A4AE";
-} else if (data.weather[0].main === "Rain") {
-  icono = "🌧️";
-  fondo = "#4FC3F7";
-} else if (data.weather[0].main === "Thunderstorm") {
-  icono = "⛈️";
-  fondo = "#616161";
-} else if (data.weather[0].main === "Snow") {
-  icono = "❄️";
-  fondo = "#E1F5FE";
-}
-
-if (climaTexto === "clear sky") {
-  climaTexto = "cielo despejado";
-} else if (climaTexto === "few clouds") {
-  climaTexto = "pocas nubes";
-} else if (climaTexto === "scattered clouds") {
-  climaTexto = "nubes dispersas";
-} else if (climaTexto === "broken clouds") {
-  climaTexto = "nublado";
-} else if (climaTexto === "overcast clouds") {
-  climaTexto = "muy nuboso";
-} else if (climaTexto === "light rain") {
-  climaTexto = "lluvia ligera";
-} else if (climaTexto === "moderate rain") {
-  climaTexto = "lluvia moderada";
-} else if (climaTexto === "thunderstorm") {
-  climaTexto = "tormenta";
-}
-
-document.body.style.backgroundColor = fondo;
-
-    resultado.innerHTML = `
-      <h2>${icono} ${data.name}</h2>
-      <p>🌡️ Temperatura: ${data.main.temp} °C</p>
-      <p>☁️ Clima: ${data.weather[0].description}</p>
-      <p>💧 Humedad: ${data.main.humidity}%</p>
-    `;
-  } catch (error) {
-    resultado.innerHTML = "Error al obtener datos 😓";
-    console.log(error);
-  }
-});
-
-inputCiudad.addEventListener("keypress", function (event) {
-  if (event.key === "Enter") {
-    btnBuscar.click();
-  }
-});
diff -ruN App-Clima-Api-original/style.css App-Clima-Api-mejorado-v2/style.css
--- App-Clima-Api-original/style.css	2026-05-12 04:55:44.329013717 +0000
+++ App-Clima-Api-mejorado-v2/style.css	2026-05-12 04:55:44.349440443 +0000
@@ -1,45 +1,303 @@
+:root {
+  color-scheme: dark;
+  --bg-a: #0f172a;
+  --bg-b: #0369a1;
+  --bg-c: #38bdf8;
+  --card: rgba(15, 23, 42, 0.64);
+  --stroke: rgba(255, 255, 255, 0.18);
+  --text: #f8fafc;
+  --muted: #cbd5e1;
+  --accent: #facc15;
+  --accent-2: #38bdf8;
+  --shadow: 0 24px 80px rgba(2, 6, 23, 0.42);
+  --radius-xl: 30px;
+  --radius-lg: 22px;
+  --radius-md: 16px;
+  --transition: 240ms ease;
+  font-family: Inter, ui-sans-serif, system-ui, -apple-system, BlinkMacSystemFont, "Segoe UI", sans-serif;
+}
+
+* { box-sizing: border-box; }
+
+html { min-height: 100%; scroll-behavior: smooth; }
+
 body {
-  font-family: Arial, sans-serif;
-  background-color: #f4f4f4;
-  display: flex;
-  justify-content: center;
-  margin-top: 50px;
+  min-height: 100vh;
+  margin: 0;
+  color: var(--text);
+  background:
+    radial-gradient(circle at top left, rgba(255, 255, 255, 0.22), transparent 28rem),
+    linear-gradient(135deg, var(--bg-a), var(--bg-b) 52%, var(--bg-c));
+  overflow-x: hidden;
+  transition: background 900ms ease;
 }
 
-.contenedor {
-  background-color: white;
-  padding: 20px;
-  border-radius: 10px;
-  width: 400px;
-  box-shadow: 0 4px 8px rgba(0, 0, 0, 0.1);
+body::before {
+  content: "";
+  position: fixed;
+  inset: 0;
+  pointer-events: none;
+  background-image:
+    linear-gradient(rgba(255, 255, 255, 0.055) 1px, transparent 1px),
+    linear-gradient(90deg, rgba(255, 255, 255, 0.055) 1px, transparent 1px);
+  background-size: 46px 46px;
+  mask-image: linear-gradient(to bottom, rgba(0, 0, 0, 0.8), transparent 80%);
 }
 
+body[data-weather="sunny"] { --bg-a: #0ea5e9; --bg-b: #38bdf8; --bg-c: #facc15; --accent: #f97316; --accent-2: #fde68a; }
+body[data-weather="cloudy"] { --bg-a: #334155; --bg-b: #64748b; --bg-c: #94a3b8; --accent: #cbd5e1; --accent-2: #e2e8f0; }
+body[data-weather="rainy"] { --bg-a: #0f172a; --bg-b: #075985; --bg-c: #22d3ee; --accent: #7dd3fc; --accent-2: #38bdf8; }
+body[data-weather="stormy"] { --bg-a: #020617; --bg-b: #312e81; --bg-c: #475569; --accent: #fde047; --accent-2: #a78bfa; }
+body[data-weather="snowy"] { --bg-a: #475569; --bg-b: #bae6fd; --bg-c: #f8fafc; --accent: #e0f2fe; --accent-2: #ffffff; }
+body[data-weather="foggy"] { --bg-a: #1e293b; --bg-b: #64748b; --bg-c: #cbd5e1; --accent: #e2e8f0; --accent-2: #f8fafc; }
+body[data-weather="night"] { --bg-a: #020617; --bg-b: #172554; --bg-c: #4c1d95; --accent: #c4b5fd; --accent-2: #93c5fd; }
+
+button, input { font: inherit; }
+button { border: 0; }
+button:disabled { opacity: 0.62; cursor: not-allowed; transform: none !important; box-shadow: none !important; }
+button:focus-visible, input:focus-visible { outline: 3px solid rgba(250, 204, 21, 0.9); outline-offset: 4px; }
+
+.sr-only {
+  position: absolute;
+  width: 1px;
+  height: 1px;
+  padding: 0;
+  margin: -1px;
+  overflow: hidden;
+  clip: rect(0, 0, 0, 0);
+  white-space: nowrap;
+  border: 0;
+}
+
+.weather-scene { position: fixed; inset: 0; z-index: -1; overflow: hidden; }
+.orb, .cloud, .rain, .sparkle { position: absolute; display: block; pointer-events: none; }
+.orb-sun {
+  width: 19rem;
+  height: 19rem;
+  top: clamp(1rem, 6vw, 5rem);
+  right: clamp(-5rem, 8vw, 7rem);
+  border-radius: 999px;
+  background: radial-gradient(circle, rgba(254, 240, 138, 0.96), rgba(251, 146, 60, 0.42) 44%, transparent 72%);
+  animation: float 7s ease-in-out infinite;
+}
+.orb-glow {
+  width: 28rem;
+  height: 28rem;
+  bottom: -10rem;
+  left: -8rem;
+  border-radius: 999px;
+  background: radial-gradient(circle, rgba(56, 189, 248, 0.44), transparent 68%);
+  animation: pulseGlow 6s ease-in-out infinite;
+}
+.cloud {
+  width: 11rem;
+  height: 3.8rem;
+  border-radius: 999px;
+  background: rgba(255, 255, 255, 0.28);
+  box-shadow: 2.8rem -1rem 0 0.6rem rgba(255, 255, 255, 0.24), 5.4rem 0.2rem 0 0.2rem rgba(255, 255, 255, 0.2);
+  opacity: 0.62;
+  animation: drift 24s linear infinite;
+}
+.cloud-one { top: 18%; left: -16rem; }
+.cloud-two { top: 44%; left: -22rem; transform: scale(1.35); animation-duration: 34s; animation-delay: -12s; }
+.cloud-three { top: 72%; left: -14rem; transform: scale(0.8); animation-duration: 28s; animation-delay: -18s; }
+.rain {
+  inset: -12rem 0 auto 0;
+  height: 120vh;
+  opacity: 0;
+  background-image: linear-gradient(180deg, rgba(186, 230, 253, 0.84) 0 56%, transparent 56% 100%);
+  background-size: 2px 72px;
+  transform: rotate(12deg);
+  animation: rainFall 650ms linear infinite;
+}
+.rain-two { background-size: 1px 48px; animation-duration: 460ms; animation-delay: -160ms; }
+.sparkle {
+  width: 0.34rem;
+  height: 0.34rem;
+  border-radius: 999px;
+  background: rgba(255, 255, 255, 0.9);
+  box-shadow: 0 0 22px rgba(255, 255, 255, 0.95);
+  opacity: 0;
+  animation: twinkle 2.6s ease-in-out infinite;
+}
+.sparkle-one { top: 16%; left: 14%; }
+.sparkle-two { top: 32%; right: 22%; animation-delay: -1.1s; }
+.sparkle-three { bottom: 24%; right: 12%; animation-delay: -1.8s; }
+body[data-weather="rainy"] .rain, body[data-weather="stormy"] .rain { opacity: 0.2; }
+body[data-weather="night"] .sparkle, body[data-weather="stormy"] .sparkle { opacity: 1; }
+body[data-weather="cloudy"] .cloud, body[data-weather="foggy"] .cloud, body[data-weather="rainy"] .cloud, body[data-weather="stormy"] .cloud, body[data-weather="snowy"] .cloud { opacity: 0.84; }
+body[data-weather="night"] .orb-sun, body[data-weather="rainy"] .orb-sun, body[data-weather="stormy"] .orb-sun, body[data-weather="foggy"] .orb-sun { opacity: 0.42; }
+
+.app-shell {
+  width: min(1120px, calc(100% - 2rem));
+  min-height: calc(100vh - 4rem);
+  margin: 0 auto;
+  padding: clamp(2rem, 5vw, 5rem) 0 1.5rem;
+  display: grid;
+  grid-template-columns: minmax(0, 0.92fr) minmax(320px, 1.08fr);
+  gap: clamp(1.2rem, 4vw, 2.4rem);
+  align-items: center;
+}
+.hero-card, .result-card {
+  position: relative;
+  border: 1px solid var(--stroke);
+  border-radius: var(--radius-xl);
+  background: linear-gradient(145deg, rgba(255, 255, 255, 0.16), rgba(255, 255, 255, 0.06)), var(--card);
+  box-shadow: var(--shadow);
+  backdrop-filter: blur(24px);
+  overflow: hidden;
+}
+.hero-card::before, .result-card::before {
+  content: "";
+  position: absolute;
+  inset: 0;
+  pointer-events: none;
+  background: linear-gradient(135deg, rgba(255, 255, 255, 0.22), transparent 36%);
+  opacity: 0.78;
+}
+.hero-card { padding: clamp(1.35rem, 4vw, 2.3rem); animation: riseIn 680ms ease both; }
+.eyebrow {
+  width: fit-content;
+  margin: 0 0 1rem;
+  padding: 0.42rem 0.8rem;
+  border: 1px solid var(--stroke);
+  border-radius: 999px;
+  background: rgba(255, 255, 255, 0.12);
+  font-size: 0.82rem;
+  font-weight: 800;
+  letter-spacing: 0.08em;
+  text-transform: uppercase;
+}
+h1, h2, p { margin-top: 0; }
 h1 {
+  margin-bottom: 0.75rem;
+  font-size: clamp(2.7rem, 8vw, 5.8rem);
+  line-height: 0.92;
+  letter-spacing: -0.08em;
+  text-wrap: balance;
+}
+.hero-copy { max-width: 45rem; margin-bottom: 1.5rem; color: var(--muted); font-size: clamp(1rem, 1.8vw, 1.12rem); line-height: 1.65; }
+.search-card { display: grid; grid-template-columns: 1fr auto; gap: 0.8rem; margin-bottom: 1rem; }
+.input-wrap {
+  display: flex;
+  align-items: center;
+  gap: 0.7rem;
+  min-height: 3.5rem;
+  padding: 0 1rem;
+  border: 1px solid var(--stroke);
+  border-radius: var(--radius-md);
+  background: rgba(15, 23, 42, 0.44);
+  transition: transform var(--transition), border-color var(--transition), background var(--transition);
+}
+.input-wrap:focus-within { transform: translateY(-2px); border-color: rgba(250, 204, 21, 0.68); background: rgba(15, 23, 42, 0.62); }
+input { width: 100%; color: var(--text); border: 0; outline: 0; background: transparent; }
+input::placeholder { color: rgba(226, 232, 240, 0.72); }
+#btnBuscar, .location-button, .chip { color: var(--text); cursor: pointer; transition: transform var(--transition), box-shadow var(--transition), background var(--transition), border-color var(--transition); }
+#btnBuscar {
+  display: inline-flex;
+  align-items: center;
+  justify-content: center;
+  gap: 0.45rem;
+  min-height: 3.5rem;
+  padding: 0 1.15rem;
+  border-radius: var(--radius-md);
+  background: linear-gradient(135deg, var(--accent), var(--accent-2));
+  color: #0f172a;
+  font-weight: 900;
+  box-shadow: 0 14px 35px rgba(2, 6, 23, 0.24);
+}
+#btnBuscar:hover, .location-button:hover, .chip:hover { transform: translateY(-3px); }
+#btnBuscar:active, .location-button:active, .chip:active { transform: translateY(0) scale(0.98); }
+.quick-actions { display: flex; flex-wrap: wrap; gap: 0.65rem; margin-bottom: 1rem; }
+.chip, .location-button { border: 1px solid var(--stroke); background: rgba(255, 255, 255, 0.11); backdrop-filter: blur(16px); }
+.chip { padding: 0.58rem 0.88rem; border-radius: 999px; font-weight: 700; }
+.location-button { display: inline-flex; align-items: center; gap: 0.5rem; padding: 0.75rem 1rem; border-radius: var(--radius-md); }
+.result-card { min-height: 500px; padding: clamp(1.1rem, 3vw, 1.8rem); animation: riseIn 800ms 120ms ease both; }
+.empty-state, .loading-state, .error-state {
+  position: relative;
+  z-index: 1;
+  min-height: 440px;
+  display: grid;
+  place-items: center;
   text-align: center;
+  padding: 1rem;
 }
-
-.busqueda {
+.empty-state > *, .loading-state > *, .error-state > * { max-width: 30rem; }
+.empty-icon, .error-icon { display: block; margin: 0 auto 1rem; font-size: 5rem; filter: drop-shadow(0 12px 24px rgba(2, 6, 23, 0.25)); animation: float 4s ease-in-out infinite; }
+.empty-state h2, .error-state h2 { margin-bottom: 0.55rem; font-size: clamp(1.6rem, 4vw, 2.2rem); }
+.empty-state p, .error-state p { color: var(--muted); line-height: 1.6; }
+.weather-current { position: relative; z-index: 1; display: grid; gap: 1.2rem; }
+.weather-main { display: grid; grid-template-columns: auto 1fr; gap: 1rem; align-items: center; }
+.weather-icon {
+  display: grid;
+  place-items: center;
+  width: clamp(6rem, 14vw, 8rem);
+  aspect-ratio: 1;
+  border-radius: 2rem;
+  background: rgba(255, 255, 255, 0.16);
+  border: 1px solid var(--stroke);
+  font-size: clamp(3.5rem, 8vw, 5.2rem);
+  box-shadow: inset 0 1px 0 rgba(255, 255, 255, 0.28), 0 18px 45px rgba(2, 6, 23, 0.22);
+  animation: weatherPop 560ms cubic-bezier(0.2, 1.24, 0.5, 1) both, float 5s ease-in-out 700ms infinite;
+}
+.city-name { margin-bottom: 0.25rem; font-size: clamp(1.8rem, 5vw, 3rem); line-height: 1; letter-spacing: -0.04em; }
+.weather-description { margin: 0; color: var(--muted); text-transform: capitalize; }
+.temperature-row {
   display: flex;
-  gap: 10px;
+  align-items: flex-end;
+  justify-content: space-between;
+  gap: 1rem;
+  padding: 1rem;
+  border: 1px solid var(--stroke);
+  border-radius: var(--radius-lg);
+  background: rgba(255, 255, 255, 0.1);
 }
+.temperature { margin: 0; font-size: clamp(4rem, 13vw, 7rem); line-height: 0.85; font-weight: 950; letter-spacing: -0.1em; }
+.temperature sup { font-size: 0.28em; letter-spacing: 0; }
+.updated-at { margin: 0; color: var(--muted); font-size: 0.92rem; text-align: right; }
+.details-grid { display: grid; grid-template-columns: repeat(4, minmax(0, 1fr)); gap: 0.75rem; }
+.detail-card, .forecast-day { border: 1px solid var(--stroke); border-radius: var(--radius-md); background: rgba(255, 255, 255, 0.1); box-shadow: inset 0 1px 0 rgba(255, 255, 255, 0.12); }
+.detail-card { padding: 0.9rem; transition: transform var(--transition), background var(--transition); }
+.detail-card:hover, .forecast-day:hover { transform: translateY(-4px); background: rgba(255, 255, 255, 0.15); }
+.detail-label { display: flex; gap: 0.4rem; align-items: center; margin: 0 0 0.35rem; color: var(--muted); font-size: 0.82rem; }
+.detail-value { margin: 0; font-size: 1.1rem; font-weight: 900; }
+.forecast-title { margin: 0.6rem 0 0; color: var(--muted); font-size: 0.88rem; font-weight: 800; letter-spacing: 0.08em; text-transform: uppercase; }
+.forecast-grid { display: grid; grid-template-columns: repeat(5, minmax(0, 1fr)); gap: 0.7rem; }
+.forecast-day { padding: 0.85rem 0.6rem; text-align: center; transition: transform var(--transition), background var(--transition); }
+.forecast-day time { display: block; color: var(--muted); font-size: 0.75rem; font-weight: 800; text-transform: capitalize; }
+.forecast-day .forecast-icon { display: block; margin: 0.55rem 0; font-size: 2rem; }
+.forecast-day strong { display: block; margin-bottom: 0.2rem; }
+.forecast-day small { color: var(--muted); }
+.loader { width: 5rem; height: 5rem; margin: 0 auto 1.4rem; border-radius: 999px; border: 0.45rem solid rgba(255, 255, 255, 0.22); border-top-color: var(--accent); animation: spin 850ms linear infinite; }
+.skeleton-line { height: 0.95rem; margin: 0.65rem auto; border-radius: 999px; background: linear-gradient(90deg, rgba(255, 255, 255, 0.12), rgba(255, 255, 255, 0.34), rgba(255, 255, 255, 0.12)); background-size: 220% 100%; animation: shimmer 1.2s linear infinite; }
+.skeleton-line:first-of-type { width: min(16rem, 70vw); }
+.skeleton-line:last-of-type { width: min(10rem, 50vw); }
+.site-footer { width: min(1120px, calc(100% - 2rem)); margin: 0 auto 1.25rem; color: rgba(248, 250, 252, 0.78); font-size: 0.88rem; text-align: center; }
 
-input {
-  flex: 1;
-  padding: 10px;
-}
+@keyframes riseIn { from { opacity: 0; transform: translateY(28px) scale(0.98); } to { opacity: 1; transform: translateY(0) scale(1); } }
+@keyframes float { 0%, 100% { transform: translateY(0); } 50% { transform: translateY(-14px); } }
+@keyframes pulseGlow { 0%, 100% { transform: scale(1); opacity: 0.78; } 50% { transform: scale(1.12); opacity: 1; } }
+@keyframes drift { from { translate: -10vw 0; } to { translate: 130vw 0; } }
+@keyframes rainFall { from { background-position: 0 0; } to { background-position: 0 72px; } }
+@keyframes twinkle { 0%, 100% { transform: scale(0.65); opacity: 0.28; } 50% { transform: scale(1.5); opacity: 1; } }
+@keyframes weatherPop { from { transform: scale(0.65) rotate(-10deg); opacity: 0; } to { transform: scale(1) rotate(0deg); opacity: 1; } }
+@keyframes spin { to { transform: rotate(360deg); } }
+@keyframes shimmer { to { background-position: -220% 0; } }
 
-button {
-  padding: 10px 15px;
-  cursor: pointer;
-  background-color: #0078d4;
-  color: white;
-  border: none;
-  border-radius: 5px;
+@media (max-width: 860px) { .app-shell { grid-template-columns: 1fr; align-items: stretch; } .result-card { min-height: 420px; } }
+@media (max-width: 620px) {
+  .app-shell { width: min(100% - 1rem, 1120px); padding-top: 0.5rem; }
+  .hero-card, .result-card { border-radius: 24px; }
+  .search-card { grid-template-columns: 1fr; }
+  #btnBuscar { width: 100%; }
+  .weather-main, .temperature-row { grid-template-columns: 1fr; text-align: center; }
+  .weather-main { place-items: center; }
+  .temperature-row { display: grid; }
+  .updated-at { text-align: center; }
+  .details-grid { grid-template-columns: repeat(2, minmax(0, 1fr)); }
+  .forecast-grid { grid-template-columns: repeat(2, minmax(0, 1fr)); }
+  .forecast-day:last-child { grid-column: 1 / -1; }
 }
-
-#resultado {
-  margin-top: 20px;
-  background-color: #eee;
-  padding: 15px;
-  border-radius: 8px;
+@media (prefers-reduced-motion: reduce) {
+  *, *::before, *::after { scroll-behavior: auto !important; animation-duration: 1ms !important; animation-iteration-count: 1 !important; transition-duration: 1ms !important; }
 }
